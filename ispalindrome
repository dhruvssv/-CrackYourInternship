class Solution:
    def isPalindrome(self, x: int) -> bool:
        reverse=""
        for i in range(1,len(str(x))+1):
            reverse+=str(x)[-i]
        if reverse==str(x):
            return True
        else:
            return False
